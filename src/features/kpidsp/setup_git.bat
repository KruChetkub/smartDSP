@echo off
git init
git remote add origin https://github.com/KruChetkub/Strategy-and-Planning-Dept.git
git add .
git commit -m "Initial commit"
git branch -M main
echo Git preparation complete. You can now run "git push -u origin main" manually if needed.
